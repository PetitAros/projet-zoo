{% extends 'base.html.twig' %}

{% block title %}index{% endblock %}

{% block body %}
    <div class="flex justify-center bg-lion bg-cover bg-center bg-no-repeat">
        <div class="flex flex-col w-80 justify-center  rounded p-4 bg-secondary-50/50 backdrop-filter backdrop-blur-sm ">
            <div class="flex justify-center">
                <img class="w-40"  src="images/LogoZoo.png">
            </div>
            <p class="text-center">Découvrez le Zoo Parc de Laval, un joyau de la biodiversité en plein cœur de la Mayenne. Rencontrez des animaux fascinants, vivez des moments uniques en famille et explorez la beauté de la nature. Une aventure inoubliable vous attend !</p>
            <div class="flex flex-col">
                <button class="bg-primary-500 rounded-2xl p-2 ">Réservez vos billets</button>
                <button class="bg-secondary-800 rounded-2xl m-3 p-1 text-background-50 backdrop-filter backdrop-blur-sm">Réservez votre hôtel</button>
            </div>
        </div>
    </div>
    <div class="flex flex-col grow">
        <div class="bg-secondary-800 grow">
            <H1 class="text-background-100 ml-3 mt-3 ">Nos Animations</H1>
                <div class="flex overflow-x-scroll pb-10 hide-scroll-bar ">
                    <div class="flex flex-nowrap lg:ml-40 md:ml-20 ml-10 ">
                        <div class="flex inline-block px-3 flex-row">
                        {% for event in events %}

                        <a href="{{ path('app_index') }}" class="bg-secondary-700 flex w-64 h-80 max-w-xs overflow-hidden rounded-lg shadow-md bg-white hover:shadow-xl transition-shadow duration-300 ease-in-out m-3 flex-col">

                                <span><img src="images/lion.png" class="w-[100%]"></span>
                                <h3 class="font-bold">{{ event.nomEvent }}</h3>
                                <p class="truncate overflow-hidden">{{ event.description }}</p>
                        </a>

                        {% endfor %}
                        </div>
                    </div>
                </div>
        </div>
    </div>

        <div class="bg-background-100 grow">
            <H1 class="text-secondary-800  ml-3 mt-3 ">Nos Animaux</H1>
            <div class="flex overflow-x-scroll pb-10 hide-scroll-bar ">
                <div class="flex flex-nowrap lg:ml-40 md:ml-20 ml-10 ">
                    <div class="flex inline-block px-3 flex-row">
                        {% for famille in familles %}

                            <a href="{{ path('app_index') }}" class="bg-secondary-700 flex w-64 h-64 max-w-xs overflow-hidden rounded-lg shadow-md bg-white hover:shadow-xl transition-shadow duration-300 ease-in-out m-3 flex-col">

                                <span><img src="images/lion.png" class="w-[100%]"></span>
                                <h3 class="font-bold text-center justify-center">{{ famille.nomFamilleAnimal }}</h3>
                            </a>

                        {% endfor %}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
